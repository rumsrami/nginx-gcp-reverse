server {
  listen 8080;
  listen [::]:8080;
  server_name api.rami.im;

  location ~ /.well-known/acme-challenge {
    allow all;
    root /usr/share/nginx/html;
  }

  root /usr/share/nginx/html;
  index index.html;
}